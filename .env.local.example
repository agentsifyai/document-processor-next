# OpenAI API Key - Required for AI-powered PDF analysis
# Get your API key from https://platform.openai.com/api-keys
NEXT_PUBLIC_OPENAI_API_KEY=your_api_key_here

# IMPORTANT: In production environments, you should NOT expose your 
# API keys in client-side code. Consider implementing a server-side API
# endpoint that securely makes the OpenAI API calls.

# Specify OpenAI model to use
# Default: "gpt-3.5-turbo" - Faster and cheaper
# Alternative: "gpt-4o" - More capable but more expensive
NEXT_PUBLIC_OPENAI_MODEL="gpt-3.5-turbo"

# Maximum PDF pages to analyze (higher = more thorough but slower & costlier)
NEXT_PUBLIC_MAX_PDF_PAGES=50